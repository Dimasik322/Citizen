#include <gtest/gtest.h>

#include <citizen/citizen.h>

#include <string>

using namespace std;

/*TEST(CitizenTests, CitizenTest1) {
    setlocale(LC_ALL, "ru");
    CitizenNamespace::Type type = CitizenNamespace::Type::Kid;
    CitizenNamespace::CitizenList Dude("Just-a-dude\0");
    Dude.add_citizen(type, 2323);
    float payment = Dude.citizens[0].get_a();
    EXPECT_EQ(payment, 33000);
}
*/